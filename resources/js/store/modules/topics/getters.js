export default{

    topics(state){
        return state.topics;
    },
    topicPosts(state){
      return state.topicPosts;
    },
    noTopicPosts(state){
      return state.noTopicPosts;
    }

}
